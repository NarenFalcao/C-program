#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
int main()
{
    int t;
    char *str;// = (char *)malloc(1000);
  //  fscanf(stdin, "%d", &t);
 //   fprintf(stdout,"%d",t);
 
  fscanf(stdin, "%s", str);
  fprintf(stdout,"%s",str);
 getch();
 
    }
